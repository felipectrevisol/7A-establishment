server:
  port: 8080

spring:
  application:
    name: establishment
    
  datasource:
      password: 7a
      username: root
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/7A?useSSL=false&serverTimezone=America/Sao_Paulo

  jpa:
    hibernate:
      ddl-auto: validate

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always